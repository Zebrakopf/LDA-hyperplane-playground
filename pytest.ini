[pytest]
testpaths = tests
# Fail on unraisable numerical warnings: an "invalid value in divide" here means a
# diagnostic is silently producing inf/nan rather than reporting `degenerate`.
filterwarnings =
    error::RuntimeWarning
